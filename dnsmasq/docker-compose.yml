services:
  dnsmasq:
    image: alpine:3.21
    container_name: dnsmasq-kornoglab
    restart: unless-stopped
    ports:
      - "53:53/udp"
      - "53:53/tcp"
    volumes:
      - ./dnsmasq.conf:/etc/dnsmasq.conf:ro
    command: sh -c "apk add --no-cache dnsmasq && dnsmasq -k"
    mem_limit: 64m
