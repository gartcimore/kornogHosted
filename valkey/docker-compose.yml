services:
  valkey:
    image: valkey/valkey:8-alpine
    container_name: valkey-kornoglab
    restart: unless-stopped
    ports:
      - "${VALKEY_PORT:-6379}:6379"
    volumes:
      - ${DATA_DIR}:/data
    mem_limit: 64m
